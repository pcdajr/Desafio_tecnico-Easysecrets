
// passar página de produtos
// await page.getByRole('button', { name: 'Next' });
// await page.getByRole('button', { name: 'Previous' });

// categorias
// await page.getByRole('link', { name: 'Phones' });
// await page.getByRole('link', { name: 'Laptops' });
// await page.getByRole('link', { name: 'Monitors' });

// adicionar ao carrinho (dentro da página do produto)
// await page.getByRole('button', { name: 'Add to cart' });

// Fechar pedido (dentro do carrinho)
// await page.getByRole('button', { name: 'Place Order' });

// remover produto (dentro do carrinho)
//getByRole('link', { name: 'Delete' });


